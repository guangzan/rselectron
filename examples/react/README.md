# React example

Learning artifact that adds `@rsbuild/plugin-react` to a three-Role application.

Rselectron has **no core React branches**. Framework support comes from Rsbuild
plugins. This directory is not a regression fixture; see `tests/fixtures/` for
automated coverage.
